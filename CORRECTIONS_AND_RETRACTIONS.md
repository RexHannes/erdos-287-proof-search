# Erdős #287 — Corrections, Retractions, and Supersessions

Append-only public correction ledger. Historical files are not deleted; later checkpoints supersede earlier controlling claims where stated.

## 31 August 2026

### RETRACTED — C0 post-conditioning index architecture

The old controlling use of

```text
n = j inverse(u') mod x
```

is retracted as the C0 architecture after canonical reduced-projective cancellation.

### RETRACTED — old C0 wrap

The old wrap

```text
1 + UU'/B
```

is retracted as the controlling finite estimate. The audited finite Fourier contraction contains the four terms

```math
\eta_b^2 \ll L^C\left(\frac{x}{B}+\frac1N+\frac1{E_b}+\frac1x\right).
```

### RETRACTED — naïve transverse two-signless DFT

The naïve full-CRT two-signless-carrier DFT is retracted. Full CRT fusion of `q_C` and `q_m` produces cross-modulus inverse coefficients in the numerator, so the transverse phase is not generally a fixed signless kernel of the previously advertised form.

### SUPERSEDED — transverse pair-existence as first frontier

```text
THREEFACTOR-TRANSVERSE-CARRIERFACTORIZATION-PAIR-EXISTENCE45
```

is preserved historically but superseded as the first transverse frontier.

The first analytic residual at that historical checkpoint was

```text
THREEFACTOR-TRANSVERSE-CRITICAL-BEZOUTNUMERATOR-SINGLECARRIER45 : OPEN
```

This is no longer the current public frontier; it remains as a dated correction record.

### SUPERSEDED AS CONTROLLING PUBLIC FRONTIER — 30 August uniform-k0 residual

The R7 label

```text
287-K0-SP2-DET1-SHAREDGCD-ONELEVEL-MOBIUS-GRAM45
```

is preserved as historical development. Supersession of the frontier is not a claim that every local identity in the old route is false.

---

## 5 September 2026 — audited R12 effectivity corrections

### RETRACTED — unweighted per-fibre `C_joint <= 0.09`

The unweighted joined covariance bound is false. A one-point physical fibre gives

```text
C_joint >= 9/64 > 0.09.
```

The surviving physical norm retains the outer `lambda(b)` weight.

### RETRACTED / SOURCE-MISMATCHED — per-prime insertion of a global singular-series factor

The global singular-series factor is not independently available at each prime coordinate of the physical source. Treating it as a per-prime multiplicative factor is not the physical normalization.

### RETRACTED — double charging `V<50` and `V<1000`

The `V<1000` finite edge replaces the earlier `V<50` charge. Retaining both is double counting.

### FIREWALL — full half-divisor chart versus incremental medium chart

These are alternative decompositions and cannot be charged simultaneously as disjoint pieces.

### FIREWALL — `Z(s,s)=0` does not by itself control directed Perron truncation

The diagonal Euler-product cancellation is an exact algebraic/analytic fact at its stated scope. It does not itself estimate the directed anti-diagonal Perron truncation, incomplete fibres, or large ratio-frequency contribution.

### FIREWALL — complete-period endpoint is not the complete joined covariance

The complete-period endpoint main is closed at its stated scope. It is not identical to the complete finite-period joined covariance.

### AUDITED R12 RESIDUAL

At the R12 checkpoint the controlling sufficient weighted covariance allowance was

```math
C_F + 2C_{ED} + C_{DD} + C_S < \frac{283}{37500}.
```

with audited capacity ledger

```text
certified subtotal:  5.257263872e-7
remaining capacity:  3.603386128e-7.
```

These remain the latest independently audited quantitative values.

---

## 5 September 2026 — later proof-map reconciliation

This section changes the **dependency interpretation**, not the audited R12 numerical bank.

### SUPERSEDED AS INDEPENDENT FRONTIERS — `F / ED / DD / S`

Later all-`q`, Ramanujan, hyperbola, and Perron dictionaries identify these as coordinate charts of a common signed physical operator. They are therefore no longer mandatory independent next theorems.

This does **not** mean the old R12 residual was separately proved. It means the current source-minimal proof search attacks the reassembled parent directly.

### SUPERSEDED — all-`q` discrepancy as the unique next theorem

Exact all-`q` resummation remains useful mathematics, but the all-`q` discrepancy is no longer the canonical top-level next node once the signed selector parent is exposed.

### SUPERSEDED / RETIRED AS AN INDEPENDENT SEQUENTIAL NODE — medium-`k`

The old sequential architecture

```text
medium-k -> two-high -> signed floor
```

is no longer controlling. Medium and higher-selector contributions are sibling pieces of one signed parent. Therefore

```text
medium-k = RETIRED AS AN INDEPENDENT SEQUENTIAL NODE.
```

This is **not** a declaration that every historical medium-`k` theorem has been proved.

### SUPERSEDED — moving/static Ramanujan frontiers as top-level controlling coordinates

The moving-Ramanujan and later static-`r` snapshots are preserved for reusable local results and research archaeology, but the current parent is the corrected selector source

```text
j=0 + j=1 + active j=2.
```

### RETRACTED — deleted-gap `a<=180`

The latest route audit marks the old deleted-gap `a<=180` claim false/retracted. A corrected range census replaces it. Do not revive the old bound.

### CANDIDATE REDUCTIONS — not yet promoted into the audited bank

The latest proof-search input reports:

```text
j>=4:   candidate-empty
j=3:    candidate-closed, |R_3| < 3e-9 B_X
active j=2:
         c=1 -> 71 prefix families
         c=2 -> 37 prefix families
```

These are recorded as **candidate** until independently audited. They change the live map without retroactively changing the R12 evidence class.

### SELECTOR DIAGONAL REPAIR — candidate scope

The full unselected `Z(s,s)=0` remains banked at its stated scope. For the active selector the live source has

```text
Z_{Theta,c}(s,s) = -H_c(s).
```

A candidate completion

```text
Ztilde_c = Z_{Theta,c} + H_c
```

restores the diagonal zero with reported finite-height auxiliary correction `<2.4e-13 B_X`.

This repairs the diagonal zero **only**. The large ratio-frequency / incomplete-fibre directed estimate remains open.

### CURRENT LIVE PARENT

```text
R_012 = signed (j=0 + j=1 + active j=2) parent
```

with live obstacles including incomplete fibres, rough defect, selector-sensitive Perron/hyperbola terms, and large ratio-frequency decay.

### DOWNSTREAM FIREWALL

```text
R_012:                    OPEN
signed floor:             OPEN
Maynard effectivization:  NOT ENTERED
finite/asymptotic splice: NOT ENTERED
Erdős #287:               OPEN
```

### PUBLICATION FIREWALL

Nothing in this correction ledger constitutes a proof of Erdős #287. Candidate results are not banked merely because they are newer, and superseded nodes are not silently classified as proved.
