# GRAVEYARD / false

These items have an actual counterexample, contradiction, or explicit retraction. They must not be revived as proof inputs without first addressing the recorded failure.

## Unweighted per-fibre joined theorem

```text
C_joint <= 0.09 uniformly for every physical fibre
```

**FALSE.** A legal one-point physical fibre forces `C_joint >= 9/64`. The physical repair retains the outer `lambda(b)` weight.

## Deleted-gap `a <= 180` claim

**FALSE / RETRACTED in the latest proof-map input.** The corrected deleted-gap range census supersedes this claim. Preserve the historical derivation for diagnosis; do not reuse the bound.

## Important distinction

Source-mismatched normalizations and invalid inferences are normally filed under `retired/`, not here, unless the mathematical statement itself has been explicitly falsified.
