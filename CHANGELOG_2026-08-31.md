# Erdős #287 public checkpoint — 31 August 2026

This checkpoint is append-only and supersedes the controlling public status where earlier files conflict. Historical R4/R5/R7 materials remain preserved.

## Research / audit changes

- C0 hostile repair completed.
- The finite unitary-Fourier closure survives the hostile repairs.
- The full unit firewall is repaired: the unit branch is `gcd(b*a_rho*b_rho*u_rho,x)=1`, and complementary nonunit cells are routed to D4.
- The small-`x` router is explicit; the finite estimate retains the `x^{-1/2}` contribution.
- Exact-product collision is analytically banked.
- Double Type II is analytically banked.
- C0 is analytically closed **conditional on the separately named formal physical-normalisation source pin**.
- The naïve transverse full-CRT two-signless DFT is retracted.
- `THREEFACTOR-TRANSVERSE-ONECONDUCTOR-RECIPROCITY45` is banked at the research/paper level.
- `THREEFACTOR-TRANSVERSE-qC-UNITARYFOURIER45` is banked at the research/paper level.
- The dense-`q_C` transverse subregion is closed on explicit hypotheses.
- The full transverse branch remains OPEN.
- The first transverse analytic residual is reduced to `THREEFACTOR-TRANSVERSE-CRITICAL-BEZOUTNUMERATOR-SINGLECARRIER45`.
- `BDIAGONAL-AFFINEA-SURVIVING-VERTEX-RECT45` remains a separate parallel OPEN branch.
- The formal normalisation remains source-pinned by `SHAREDGCD-OMEGAH-NORMALISATION-SOURCEPIN45` plus the complete physical Perron/nuclear normalisation unless absorbed into the same pin.
- Erdős Problem #287 remains OPEN.

## Formal / Aristotle checkpoint

The supplied 31-August Aristotle bundle records a completed C0 unitary-Fourier / transverse-carrier-interface bank with:

```text
default lake build: PASS
8298 jobs
0 errors
```

and axiom reports containing only ordinary Mathlib foundations (`propext`, `Classical.choice`, `Quot.sound`) for the audited declarations, with no reported `sorry`, custom axiom, `unsafe`, opaque shortcut, `implemented_by`, or `native_decide`.

The named newest files depend on a larger `CurrentProgramme` / hostile-audit graph that is not yet dependency-complete in the public main tree. Therefore this checkpoint records the verified attached formal bank without falsely claiming that the public `RequestProject/Main.lean` has already reproduced the same 8298-job build.

No newer transverse one-conductor formal bank is claimed unless a completed, dependency-complete, kernel-checked run is actually available.

## Public status after this checkpoint

```text
ERDOS287: OPEN
C0: ANALYTICALLY CLOSED / CONDITIONAL ON FORMAL NORMALISATION
EXACT PRODUCT COLLISION: ANALYTICALLY CLOSED
DOUBLE TYPE II: ANALYTICALLY CLOSED
TRANSVERSE LONG-CONDUCTOR: STRICTLY REDUCED / OPEN
FIRST ANALYTIC RESIDUAL: THREEFACTOR-TRANSVERSE-CRITICAL-BEZOUTNUMERATOR-SINGLECARRIER45
PARALLEL b-DIAGONAL: OPEN
```
