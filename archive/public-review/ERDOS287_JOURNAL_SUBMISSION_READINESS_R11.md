# Erdős #287 — R11 Journal Submission Readiness

**Checkpoint:** 2 September 2026  
**Controlling source:** `ERDOS287_JOURNAL_MASTER_R11_02SEP2026.tex`  
**Public mathematical verdict:** **ERDŐS #287 REMAINS OPEN.**

## Bottom-line submission classification

The repository is **not ready for submission as a claimed proof of Erdős Problem #287**.

It is substantially closer to being suitable as a **research/preprint article presenting**:

- a machine-checked finite reduction and exclusion through `M <= 4,000,000,000`;
- an exact conditional `WindowPairSupply` large-`M` compiler;
- a self-contained Gate 0 / Gate 1A / Gate 1B / Gate 2 theory manual;
- the Ford–Maynard two-lane source architecture;
- paper-level progress on `E_T`, `N_2`, `Bsrc/N1/E_M`, Gate 1A, Gate 1B, and the hard Ford other-parent source;
- an explicit list of the remaining mathematical, source/compiler, formal, and effectivity nodes.

The R11 master deliberately does **not** advertise any of those open nodes as proved.

## Why R9/R10 alone were no longer enough

R9 retained the full Gate 0–2 theory manual and was already designed to be self-contained, but later hostile audits changed the current frontier materially.

R11 incorporates the following corrections and later results:

1. **Tot/U source split.** The full correlation uses the Lemma-7.21 Type-I lane; Proposition 7.22 is the leakage `U` lane. Selected-`E` data exist only on `U`.
2. **`E_T` update.** The Tot lane is paper-level closed directly by Type I.
3. **Physical `N_2` Lambda collar.** The actual von-Mangoldt-weighted collar is paper-level closed at fixed epsilon, with `E_2/B_X <= K_collar epsilon + o(1)`; it is not falsely strengthened to `E_2=o(B_X)` for fixed epsilon.
4. **`Bsrc/N1/E_M`.** These are recorded as paper-level asymptotic results, not Lean analytic theorems.
5. **`b`-diagonal downgrade.** The later hostile audit retracts broad local-kernel closure: Archimedean nuclear mass costs `L^C`, the small-`d` quadratic cost is `Y^2`, the large-`d` weighted estimate requires the corrected `L^2` ratio, and Perron/rectangle/`Omega_H` source obligations remain.
6. **Gate 1A.** The scalar-`xi`/rowwise-`L^1` architecture is superseded by the literal physical `t`-state Hilbert formulation; the source ceiling is paper-level closed but the synchronized centered frame remains open.
7. **Gate 1B.** Pure-Möbius cells are paper-level closed; canonical-block and Möbius-free reciprocal cells remain open.
8. **Shared Ford other-parent.** The hard `U`, `k>=1` family is separated from project-specific downstream claims. It is a common Ford-generated theorem with independent adapters to #287 and the Twin/HSTAR programme.
9. **Effectivity.** `K_pert=0` for the separated `N_1` architecture, several explicit constants are available, but the uniform explicit two-linear-form sieve and the final threshold remain open.

## Journal-standard content now included in R11

R11 is a single self-contained TeX source with:

- exact problem statement and claim-status box;
- exact finite/large-`M` interface;
- physical shifted-prime source and error ledger;
- Gate 0;
- latest Gate 1A;
- latest Gate 1B;
- two-lane Ford source and shared other-parent theorem;
- physical `N_2` and `Bsrc/N1/E_M` sections;
- hostile-audited `b`-diagonal corrections;
- Gate 2 / FCL assembly;
- effectivity / finite splice;
- formal-verification and semantic-hygiene section;
- exact dependency graph;
- explicit proved/not-proved lists;
- reviewer falsification checklist;
- primary bibliography.

## Twin Gate 1A / 1B firewall

Gate 1A and Gate 1B are retained because they are part of the broader source/provider theory and share Ford–Maynard geometry with the #287 programme.

They are **not** presented as automatically proved inputs to the current shortest #287 FCL chain.

The shared `k>=1` Ford other-parent theorem must live in a neutral mathematical layer with two independent adapters:

```text
shared Ford theorem
    -> Twin/HSTAR adapter
    -> #287 hard-U/E_L adapter
```

No Twin Prime conclusion is a hypothesis of the #287 article.

## Claim firewall for submission

### Safe now, with exact qualification

- “Machine-checked finite exclusion through `M <= 4e9`.”
- “Kernel-checked conditional `WindowPairSupply` compiler.”
- “Self-contained conditional large-`M` framework.”
- “Paper-level closure of the Tot/Type-I `E_T` lane.”
- “Paper-level fixed-epsilon physical `N_2` collar.”
- “Paper-level asymptotic `Bsrc/N1/E_M` comparison.”
- “Paper-level Gate 1A Hilbert source ceiling.”
- “Paper-level Gate 1B pure-Möbius subfamily.”
- “Explicitly isolated hard Ford other-parent, b-diagonal, and effectivity residuals.”

### Unsafe now

- “Erdős #287 is proved.”
- “Full local analytic kernel is closed.”
- “b-diagonal is closed.”
- “Gate 1A is closed.”
- “Gate 1B is closed.”
- “`E_L` is closed.”
- “Asymptotic FCL is unconditional.”
- “The final effective threshold is known.”
- “The full analytic proof is Lean-verified.”

## Minimum work before a solved-proof submission

A submission claiming a solution requires all of the following:

1. close or bypass the hard `U`, `k>=1` shared Ford other-parent theorem and obtain the physical `E_L` bound;
2. repair the `b`-diagonal Perron total-nuclear ledger, source the rectangle inequality, and resolve proof-local `Omega_H` provenance (or prove that the final #287 chain does not consume this owner);
3. complete the final four-error FCL source audit with no duplicated or omitted packets;
4. extract an explicit large-`X` threshold and prove an `M0` that overlaps the finite verified range, or extend the finite verification to the final `M0`;
5. run a full source-to-paper hostile audit of the frozen manuscript;
6. migrate/rebuild the final dependency spine on current stable Lean;
7. run `#print axioms`, fresh Lean proof replay/checking, and independent comparator/external-checker validation;
8. independently compare the trusted formal challenge statement with the mathematical statement submitted to the journal;
9. remove every unresolved external research socket from any claim labelled “formally verified proof”.

## Minimum work before a research-note / conditional-criterion submission

For a submission explicitly framed as **open research / conditional criterion / verification programme**, the main remaining editorial tasks are:

1. compile R11 and inspect the PDF for notation, cross-reference and typography errors;
2. add exact page/theorem citations to Ford–Maynard wherever the current draft refers to the source architecture;
3. independently proofread every displayed equation copied from the latest research reports;
4. attach or archive the exact Lean commit and build log supporting the finite theorem;
5. include the hostile-audit status table as supplementary material;
6. state prominently in the abstract, introduction and conclusion that #287 remains open.

Under that framing, R11 is designed to be publication-safe: open nodes are exposed, not hidden.

## Recommended frozen submission bundle

```text
ERDOS287_JOURNAL_MASTER_R11_02SEP2026.tex
ERDOS287_JOURNAL_SUBMISSION_READINESS_R11.md
CURRENT_STATUS.md
CORRECTIONS_AND_RETRACTIONS.md
ERDOS287_PUBLIC_FORMAL_RECONCILIATION_2026-09-01.md
TRUSTED_BANK_AUDIT.md
RequestProject/Erdos287/ProblemStatement.lean
RequestProject/Erdos287/ClosureInputs.lean
finite-range certificate modules used by the final theorem
latest hostile-audit reports for b-diagonal, N2, 287A, and semantic cleaning
```

## Final readiness verdict

**As a solved-proof paper:** **NOT READY.**

**As a self-contained research/preprint article with an explicit conditional criterion and open-frontier ledger:** **R11 is the correct current master, subject to compilation and an independent mathematical copy-edit/source audit.**
