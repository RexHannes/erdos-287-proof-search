# Aristotle / Lean summary — Erdős Problem #287

**Current checkpoint:** 1 September 2026  
**Public theorem status:** **ERDŐS #287 REMAINS OPEN.**

This is the living high-level formalization summary. Older Aristotle run reports remain in Git history and dated safe-bank reports for provenance; they do not override this file.

## Current formal entrypoint

Use:

```text
RequestProject/CurrentErdos287.lean
```

It imports the established #287 `RequestProject.Main` spine and the newest source-specific direct3221 / K0-SP2 status, finite-algebra, interface, log-budget and axiom-audit layer.

It does **not** import Gate-1A / Gate-1B / Twin-Prime source modules.

## New 1-Sep research status represented safely

The exact physical `2+5` dictionary is

```text
m = p1*p2
w = p3*p4*p5*p6*p7
q | 2*m*w+s  <->  w = -s*(2*m)^(-1) mod q.
```

Research/paper estimate for the physical Balanced7 super-square-root band:

```text
per dyadic Q:
    |D_{Q,s}| << X L^(-5/2+o(1))

all super-square-root dyadic Q:
    sum_Q |D_{Q,s}| << X L^(-3/2+o(1)) = o(X/L).
```

Accordingly:

```text
SP2-BALANCED7-SUPERSQRT-DIRECT3221-DEFECT45:
    PAPER / RESEARCH CLOSED

SP2-BALANCED7-FULL-Q45:
    PAPER / RESEARCH CLOSED

BALANCED7:
    PAPER / RESEARCH CLOSED
```

The stronger generic arbitrary-`A` target

```text
X L^(-A)  for every fixed A
```

remains **OPEN** and is kept as a different type/interface.

## Current research frontier

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45:
    OPEN

FULL-SOURCE-LOCAL-ANALYTIC-KERNEL45:
    OPEN

WindowPair:
    OPEN / not yet shown unnecessary

ERDOS287:
    OPEN
```

The direct physical estimate has logarithmic exponent `-3/2+o(1)`, while the final physical criterion requires a net exponent strictly below `-1`. Hence naive absolute source reassembly has only

```text
C_total < 1/2
```

additional logarithmic budget.

## New Lean layer

The 1-Sep delta formalizes only deterministic / kernel-safe material:

- direct centered-defect finite data;
- exact raw/raw shift geometry;
- separate uninhabited short-shift-sieve and Shiu-style analytic-provider interfaces;
- generic-versus-physical endpoint type firewall;
- K0-SP2 fragment metadata and owner/reassembly interfaces;
- symbolic half-log budget lemmas;
- current status and `#print axioms` audit modules.

It does not manufacture external analytic number theory as an axiom and does not inhabit the final K0-SP2 reassembly input.

## Project-contamination firewall

The current #287 formal entrypoint imports only the established #287 spine plus the new #287 Sep-1 modules.

Older files with historical `Gate1A` / `Gate1B` names exist in the repository from earlier mixed research-bank work. They are **legacy/noncontrolling**: `RequestProject/Main.lean` does not import them, `RequestProject/CurrentErdos287.lean` does not import them, and CI checks the living entrypoints/current status for cross-project active labels.

Source-neutral generic lemmas may remain in legacy files for provenance, but they carry no current #287 theorem ownership, source realization, analytic input or compiler role.

The separate Twin-Prime programme is maintained in `RexHannes/twin-prime-proof-search` and is not an active dependency of this formal entrypoint.

## Formal discipline

- no `sorry` / `admit` / custom analytic axiom in the new closure layer;
- no `unsafe`, `native_decide` or `implemented_by` shortcut;
- analytic provider interfaces remain explicit and uninhabited unless genuinely formalized;
- paper/research closure is not relabelled as kernel proof;
- physical Balanced7 closure is not promoted to the generic arbitrary-`A` theorem;
- no local source family is promoted to the complete #287 source census without the explicit reassembly compiler.

**ERDŐS PROBLEM #287 REMAINS OPEN.**
