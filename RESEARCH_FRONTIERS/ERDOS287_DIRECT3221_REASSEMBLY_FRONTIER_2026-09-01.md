# Erdős #287 — Direct3221 / K0-SP2 Reassembly Frontier

**Date:** 1 September 2026  
**Problem status:** OPEN.

## Banked research-level physical endpoint

For the literal Balanced7 `2+5` source,

```text
m = p1 p2
w = p3 p4 p5 p6 p7
```

and

```math
q \mid 2mw+s
\iff
w \equiv -s(2m)^{-1} \pmod q.
```

The direct centered raw-minus-principal variance route yields

```math
|D_{Q,s}| \ll X L^{-5/2+o(1)}
```

per dyadic super-square-root cell and

```math
\sum_Q |D_{Q,s}| \ll X L^{-3/2+o(1)} = o(X/L)
```

after summing the band.

Research classification:

```text
SP2-BALANCED7-SUPERSQRT-DIRECT3221-DEFECT45: PAPER/RESEARCH CLOSED
SP2-BALANCED7-FULL-Q45: PAPER/RESEARCH CLOSED
BALANCED7: PAPER/RESEARCH CLOSED
```

The generic arbitrary-`A` socket remains open.

## Current controlling research node

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45
```

Required audit:

1. recover the exact full K0-SP2 source partition;
2. prove source exhaustivity and overlap coefficients;
3. assign each fragment one analytic owner;
4. calculate the exact total log-cost exponent;
5. ensure the net exponent stays strictly below `-1`;
6. if naive reassembly costs at least `L^(1/2)`, recombine before triangle inequality or use a joint square-sum/cancellation mechanism.

## Half-log firewall

```text
base physical exponent:    -3/2
required final exponent:   < -1
naive added cost allowed:  < +1/2
```

Therefore a statement such as `O(polylog X)` fragments is not sufficient by itself.

## Downstream graph

```text
Balanced7 physical endpoint
    -> K0-SP2 uniform fragmentation/reassembly [OPEN]
    -> FULL-SOURCE-LOCAL-ANALYTIC-KERNEL45 [OPEN]
    -> exact WindowPair/direct-final compiler audit
    -> ERDOS287 [OPEN]
```

WindowPair is not assumed necessary until the exact repository implication graph is audited.
