# Route Graveyard

This file is a compact index of routes that should not be accidentally resurrected as current claims. Detailed dated evidence remains in [`../CORRECTIONS_AND_RETRACTIONS.md`](../CORRECTIONS_AND_RETRACTIONS.md) and [`../archive/historical-reports/`](../archive/historical-reports/).

| Route / claim | Disposition | Reason |
|---|---|---|
| C0 post-conditioning index `n = j inverse(u') mod x` | **RETIRED** | superseded by canonical reduced-projective cancellation |
| old C0 wrap `1+UU'/B` | **RETIRED** | audited finite Fourier contraction has a different four-term capacity |
| naïve transverse two-signless full-CRT DFT | **RETIRED** | cross-modulus inverse coefficients survive in the numerator |
| unweighted per-fibre `C_joint <= 0.09` | **FALSE** | one-point physical counterexample gives at least `9/64` |
| per-prime insertion of global singular-series factor | **SOURCE-MISMATCHED** | global factor is not independently available at each prime coordinate |
| keep both `V<50` and replacement `V<1000` charges | **INVALID** | double counting |
| combine full half-divisor and incremental medium charts | **INVALID** | alternative, not disjoint, decompositions |
| `Z(s,s)=0` => directed Perron anti-diagonal control | **INVALID INFERENCE** | finite diagonal Euler algebra does not provide the contour/tail theorem |
| complete-period endpoint = complete joined covariance | **INVALID INFERENCE** | endpoint main omits the surviving physical weighted covariance |
| 2 Sep R10 two-lane `E_T/E_L` frontier as current status | **SUPERSEDED** | R12 effectivity synthesis is now the audited baseline |

## Rule

A retired route may still contain correct local identities. `RETIRED` means it must not be reused as the controlling physical closure without first repairing the exact stated defect and passing source/capacity audit.
