# R12 Changelog — 5 September 2026

R12 is a source-reconciled journal rewrite of the current Erdős #287 effectivity bank. It does not promote any open node.

## Added or materially revised

- full theorem–lemma–proof exposition rather than a progress memo;
- explicit seven-level verification vocabulary and status table;
- authoritative source-precedence and discrepancy table;
- physical `B_src` normalisation with the global singular-series factor exactly once;
- exact odd-squarefree Möbius collapse and all-complement identities;
- corrected odd half-divisor chart, `w=6` parity firewall, and full-versus-medium ledger firewall;
- exact `c=1/c=2` splice, including both factors `1/2`;
- complete odd-line cancellation, secondary-pole socket, and strict equality correction;
- two-variable `Z(u,v)`, finite and analytic diagonal cancellation, residues, and Perron Jacobian;
- explicit anti-diagonal Perron obstruction;
- exact hostile counterexample retiring the unweighted `C_joint <= 0.09` theorem;
- corrected `lambda(b)`-weighted aggregate criterion;
- deterministic closure of the full `V<1000` edge;
- exact Farey covariance identity and physical all-`b` Euler resummation;
- complete-period endpoint main `<469/75000` and residual allowance `283/37500`;
- explicit `F/ED/DD/S` covariance frontier with the cross term counted twice;
- exact all-`q` resummation and 800 rational regression checks;
- exact replacement ledger yielding subtotal `5.257263872e-7` and capacity `3.603386128e-7`;
- downstream guards for medium-`k`, two-high, signed floor, Maynard, and the public problem;
- reviewer checklist, dependency graph, certificate table, and formal module map.

## Retired or superseded

- per-prime use of the global physical singular-series factor;
- the unweighted per-fibre joined theorem;
- the old `V<50` edge charge;
- treating the earlier historical kernel subtotal as the live directed subtotal;
- treating `Z(s,s)=0` as a stand-alone anti-diagonal Perron estimate;
- treating the complete-period endpoint main as the whole covariance;
- combining the full half-divisor replacement ledger with the incremental medium ledger.

## Unchanged global verdict

Medium-`k` is strictly reduced but open. Two-high has not been entered. Signed floor is open. Maynard effectivisation has not been entered. Erdős Problem #287 remains open.
