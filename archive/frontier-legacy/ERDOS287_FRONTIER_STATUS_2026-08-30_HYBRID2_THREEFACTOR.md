# Erdős #287 — Frontier Snapshot — 30 August 2026

**Status:** `ERDOS287 OPEN`

This is an append-only research snapshot. It does not supersede formal theorem provenance and does not claim closure.

## Formal/source first residual

```text
287-K0-SP2-DET1-
SHAREDGCD-OMEGAH-
NORMALISATION-SOURCEPIN45
```

`lambda_H = mu * Omega_H` is banked; the literal `Omega_H(e)=W(e/H)` normalisation/support/packet constants remain source-pinned. Polynomial-`B` / SMALL-`B` and polynomial-`G` margins remain parallel source-margin owners where invoked.

## Hybrid 2

```text
DET1-FUSED-
DELTA-MOBIUS-RECIPROCAL-
HYBRID2-45: PASS
```

Hostile-audited / repaired inputs:

```text
SHORT-D: CLOSED
SHORT-M: CLOSED
SHORT-Q: CLOSED via SHORT-Q ⊂ SHORT-M
large d2: CLOSED
g0 g0' | e(b2-b1): PASS
lambda_arch = O(1): PASS
beta pointwise packet: << (loglog X)^2 = L^o(1)
Archimedean smooth separated packet: PASS
```

The standard separated-frequency additive large sieve is the local analytic theorem input. `Omega_H` remains an outer source pin rather than a local Hybrid-2 dependency.

## Current long-edge off-diagonal child

Let

```math
D_b=b_2-b_1,
\qquad
z=(b_1,m_2),
\qquad
d_2=(m_2,D_b).
```

The audited conductor reduction is

```math
Q_1^\circ=z^2q_Cq_m,
```

with

```math
q_C
=2\frac e{(e,N_1)}
  \frac{r_2}{(r_2,D_b)}
  b_1^\flat,
\qquad
q_m=m_2^\flat.
```

Short `q_C` and short `q_m` are routed. The surviving long-edge packet has the ultra-near determinant restriction

```math
\mathcal D
\ll
eB L^C
\sqrt{\frac{R_{\rm aff,0}}{g_0g_0'}}.
```

The simultaneous reciprocal phases leave a moving-modulus matrix: the `q_m` phase sees `Delta D_b/d_2`, while the `q_C` phase retains separate dependence on `Delta`, `b_1`, and `m_1`.

Therefore the first analytic residual, conditional on outer source normalisations, is

```text
287-K0-SP2-DET1-
FUSED-SMALL-AFFINEDEFECT-
COMMONE-SMALLH-
HYBRID2-LONGEDGE-
THREEFACTOR-MOVINGMOD-
MATRIX45
```

## Parallel b-diagonal child

```text
287-K0-SP2-DET1-
FUSED-SMALL-AFFINEDEFECT-
BDIAGONAL-LONGQC-
PRODUCTMOD-RECT45
```

Short `q_c` is routed to SMALL-`B`; the long-conductor product-modulus rectangle remains open.

## Next attack

1. Hostile-audit `Q_1^circ=z^2 q_C q_m` and all pairwise coprimalities / inverse units.
2. Test reciprocal fusion between the `q_C` and `q_m` phases.
3. If no full fusion occurs, expose the genuine two-parameter frequency in `(Delta, Delta D_b/d_2)`.
4. Attack the resulting source-weighted two-parameter moving-modulus matrix while preserving the opposite Möbius sign.

## Publication firewall

```text
ERDOS287: OPEN
```
