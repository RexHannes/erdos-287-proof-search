# 1 September 2026 — Direct3221 / K0-SP2 Reassembly Checkpoint

## Research-status change

The previous public checkpoint treated the super-square-root centered q-cell as a controlling analytic frontier. The latest source-specific calculation separates the generic reusable socket from the actual Balanced7 endpoint.

New status:

```text
Generic arbitrary-A supersqrt socket:
    OPEN.

Physical Balanced7 direct3221 supersqrt endpoint:
    PAPER/RESEARCH CLOSED.

SP2-BALANCED7-FULL-Q45:
    PAPER/RESEARCH CLOSED.

BALANCED7:
    PAPER/RESEARCH CLOSED.

Current first research residual:
    287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45.

ERDOS287:
    OPEN.
```

## Analytic mechanism

The physical `2+5` grouping maps exactly to the inverse-sampled 3221 residue dictionary. The direct proof estimates the complete centered raw-minus-principal defect before the old V21 low/high conductor split. It therefore does not require separate `AA/BA/AB/BB` ownership, V20 HHH closure, Wright, Pascadi `3+2+2`, or well-factorable Maynard input for this physical cell.

The research-level endpoint is

```math
|D_{Q,s}| \ll X L^{-5/2+o(1)}
```

per dyadic cell and

```math
\sum_Q |D_{Q,s}| \ll X L^{-3/2+o(1)} = o(X/L)
```

after the super-square-root dyadic band is reassembled.

## Strength firewall

The direct physical endpoint does **not** prove the stronger generic theorem `X L^{-A}` for every fixed `A`. That generic socket remains open.

## New reassembly issue

The physical endpoint has a half-log reassembly margin. Any complete K0-SP2 source census must therefore record the exact logarithmic exponent of fragmentation, leakage, dyadic, Perron/Mellin and labelled-slot costs. Naive absolute reassembly is sufficient only if the total added exponent is strictly below `1/2`.

## Formal update

Added append-only status/interface modules:

- `RequestProject/Status/CurrentStatusErdos287Direct3221Reassembly.lean`
- `RequestProject/CurrentProgramme/Erdos287K0SP2LogBudget.lean`
- `RequestProject/CurrentProgramme/Erdos287Direct3221Interfaces.lean`
- `RequestProject/Status/AxiomAuditErdos287Direct3221Reassembly.lean`

The external analytic sieve/divisor-average providers remain uninhabited interfaces rather than axioms.

## Public status

**ERDŐS PROBLEM #287 REMAINS OPEN.**
