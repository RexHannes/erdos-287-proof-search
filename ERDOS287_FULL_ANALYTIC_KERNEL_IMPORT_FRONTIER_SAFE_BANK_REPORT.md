# ERDŐS #287 — FULL ANALYTIC KERNEL IMPORT FRONTIER (append-only safe bank)

**Erdős #287 is NOT claimed proved.**  Nothing in this layer proves, assumes, or
manufactures any external analytic theorem.  Every analytic conclusion reported
by the paper/research run is carried as a **field of an uninhabited typed
structure**, and every downstream theorem exposes it as an explicit hypothesis.

All previous files and status layers are preserved unchanged; this layer is
appended strictly later than `regularPerronSourceFrontier`.

---

## Files added (append-only)

| File | Content |
|---|---|
| `RequestProject/CurrentProgramme/Erdos287FullSourceAnalyticKernelInput.lean` | §2 — the typed external analytic input, explicit quantitative fields, counterguards |
| `RequestProject/CurrentProgramme/Erdos287MasterSourceTypedPerronPackets.lean` | §3–§5 — the compiler chain, the de-regularisation identity, the owner type and the owner partition |
| `RequestProject/CurrentProgramme/Erdos287OneBoundedSourceFactor.lean` | §6 — the algebraic one-bounded factorisation `A·B`, Möbius linearity in `B` |
| `RequestProject/CurrentProgramme/Erdos287DeterminantOneCompiler.lean` | §7 — `rq − 2ab = s` → `b = b₀ + rt`, `q = q₀ + 2at` |
| `RequestProject/CurrentProgramme/Erdos287TwoCopyRouterAndOmega.lean` | §8–§9 — the exact `Δ` router and the proof-local shared-gcd `Ω` partition |
| `RequestProject/CurrentProgramme/Erdos287FullAnalyticKernelFCLChannels.lean` | §10–§15 — analytic input → correlation bound, physical source pin, `E_T/E_L/E_2/E_M`, reuse firewalls |
| `RequestProject/Status/CurrentStatusErdos287FullAnalyticKernelImportFrontier.lean` | §1, §16 — the new status layer `fullAnalyticKernelImportFrontier` |
| `RequestProject/Status/AxiomAuditErdos287FullAnalyticKernelImportFrontier.lean` | §17 — `#print axioms` on every principal new theorem |
| `RequestProject/Main.lean` | imports of the eight new modules appended at the end of the import block |

---

## STRICT FINAL REPORT

```
LAKE BUILD:
PASS

JOB COUNT:
8370

NEW WARNINGS:
none in the new modules (pre-existing warnings in older files unchanged)

SORRYAX:
none

CUSTOM AXIOMS:
none
(87 audited new declarations depend only on
 [propext, Classical.choice, Quot.sound] or a subset;
 11 depend on no axioms at all)

FULL ANALYTIC KERNEL STATUS:
EXTERNAL / PAPER-BANKED

ANALYTIC INPUT:
UNINHABITED
(FullSourceLocalAnalyticKernelInput; refutable at explicit data by
 analyticInput_is_a_genuine_constraint; not derivable from the finite/source
 combinatorics by analyticInput_not_from_source_combinatorics)

MASTER-SOURCE-TO-TYPED-PERRON-PACKETS45:
KERNEL-PROVED (source / formal compiler)

OWNER PARTITION:
PASS
(exactly six owners; one owner per packet; no packet with two owners;
 fibres disjoint; exhaustive; exact owner-wise reassembly;
 Ω inserted strictly after the two-copy stage)

DET1 COMPILER:
PASS
(det1_shift, det1_isCoprime, det1_base_of_isCoprime, det1_param_complete,
 det1_solution_line, det1_t_is_unique)

TWO-COPY ROUTER:
PASS
(exhaustive and disjoint; Δ = t₁n₂ − t₂n₁; no analytic closure from a tag)

PROOF-LOCAL OMEGA:
PASS
(1 = ∑_{H ≤ K} Ω_H(gcd(g₁,g₂)); genuine two-copy coordinate)

PHYSICAL P/N1/N2/U:
SOURCE-PIN OPEN
(typed interface PhysicalCertificateSource + adapter toData is kernel-proved;
 the pin predicate PhysicalSourcePinned is UNINHABITED — no literal repository
 source field was invented)

E_T:
DEFINED

E_L:
DEFINED

E_2:
DEFINED

E_M:
DEFINED

FCL:
OPEN
(only the combination identity E_T + E_L + E_2 + E_M is proved; the budget
 inequality is an explicit, unconstructed antecedent of
 fcl_prime_mass_pos_of_channels)

FCL->WINDOWPAIR:
KERNEL-PROVED CONDITIONAL
(reused verbatim from windowPairSupply_of_positiveFCLMass; not duplicated)

EFFECTIVE WINDOWPAIR:
UNINHABITED
(AsymptoticWindowPairSupply kept strictly distinct from
 EffectiveWindowPairSupply; the M0 ≤ 4,000,000,000 field is retained separately)

ERDOS287:
OPEN

FIRST EXACT FORMAL/SOURCE RESIDUAL:
the literal physical fixed-certificate source pin
`Erdos287.FullAnalyticKernelFCL.PhysicalSourcePinned`
(and, immediately after it, the FCL channel budget
 `ch.total ≤ (1 + Cc)/6 · B`), NOT the regular-Perron analytics.

PROOF CLAIM CERTIFIED:
NO

STOP.
```

---

## Recorded research status (metadata only, no proof implication)

The paper/research reports
`FULL-SMOOTH-MINUS-CLOSED-SECTORS45`, `COMPLETE-PREFIX-ROUGH-FACTOR45`,
`PERRON-PARENT-1BOUNDED-FACTOR45`, `K0-SP2-LOWQ-BILINEAR-LS45`,
`PASCADI63-GENERATED-TRIPLE-CELLS45`, the `C0`, `TRANSVERSE` and `b-DIAGONAL`
fixed packets, `287-K0-SP2-REGULAR-PERRON-SMOOTH-MOBIUS-CORRELATION45`,
`287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45` and
`FULL-SOURCE-LOCAL-ANALYTIC-KERNEL45` are recorded as **paper/research
pass/closed**, i.e. `analyticBanked` in the ledger.  They are **not** Lean
theorems.  The Lean ledger firewall
`analyticBanked_is_not_kernelProved` makes this distinction machine-checked.
