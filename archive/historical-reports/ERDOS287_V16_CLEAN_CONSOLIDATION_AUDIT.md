# Erdős Problem #287 — V16 Clean Consolidation Audit

**Audit date:** 28 August 2026  
**Verdict:** the previous V15 PDF was a valuable technical dossier, but not an optimal public paper. V16 separates the coherent public theorem/criterion note from the chronological research ledger and preserves V15 immutably.

## 1. Materials audited

1. Public repository `RexHannes/erdos-287-proof-search`, including:
   - the exact problem statement and finite compiler;
   - `FiniteRangeExtension.lean`;
   - `ClosureInputs.lean`;
   - the public V14 status file;
   - the V15 patch-stack PDF/source and the public README/frontier.
2. The supplied midpoint Aristotle archive `midway.gz`:
   - SHA-256: `609b139eb09a69b6ed39654e661dd3ea50d0e590eee371dbed18d014f4b5cb4e`;
   - 92 Lean source files after extraction;
   - V15 `μ*log`, determinant-one-line, and balanced-seven files;
   - V15 status source with deliberately uninhabited analytic interfaces.
3. The latest balanced-seven source audit, including the exact polarization, collision router, range death tests, signed endpoint residual, and comparison pin.

## 2. Controlling status after audit

| Layer | Audit status | Controlling interpretation |
|---|---|---|
| Exact public counterexample predicate | **PUBLIC LEAN-CHECKED** | The set form and ordered sequence form are connected explicitly. |
| No counterexample with maximum `M <= 4,000,000,000` | **PUBLIC LEAN-CHECKED** | This is the strongest unconditional result in the project. |
| `WindowPairSupply` and end-to-end closure compiler | **PUBLIC LEAN-CHECKED / CONDITIONAL** | Eventual large-`M` supply plus the finite range implies the public statement. |
| V14 Vaughan affine-source spine | **PUBLIC LEAN-CHECKED ALGEBRA** | Structural source reduction only; analytic children remain open. |
| V15 `Λ = μ * log`, hard-pair line, balanced-seven polarization | **FORMALIZATION SOURCE PRESENT / PUBLIC REPLAY PENDING** | Exact mathematics is written out, but public `main` did not yet contain V15 Lean at the cutoff and this environment could not rerun `lake`. |
| Polarized signed endpoint estimate | **OPEN ANALYTIC** | `AFFINE287-POLARIZED-OMEGA7-SIGNED-EOD45`. |
| Comparison/low-conductor match | **OPEN SOURCE** | `AFFINE287-MULOG-COMPARISON-LOWCOND-MATCH45`. |
| Remaining hard `μ`-log cells / other fixed-certificate packets | **OPEN** | Balanced-seven closure alone is not FCL closure. |
| Eventual `WindowPairSupply` | **OPEN** | The exact global large-`M` input remains unproved. |
| Erdős #287 | **OPEN** | No solution claim. |

## 3. Lean audit boundary

### Public bank

The public repository contains the exact finite theorem and the V14 source status. The V14 report records a successful `lake build` with 8111 jobs and a source scan without Lean-code uses of:

- `sorry`;
- `admit`;
- a user `axiom`;
- `opaque`;
- `unsafe`;
- `native_decide`;
- `@[implemented_by]`.

### Midpoint V15 source

The midpoint archive contains the expected V15 modules and an explicit status file. A static scan of the supplied Lean source found no prohibited declaration above, and the analytic interfaces are structures/specifications without inhabitants.

The audit does **not** promote V15 to public Lean-checked because:

1. the files were absent from public `main` at the cutoff;
2. the user reported that the Lean run was still in progress;
3. the archive's top-level summary clearly records the completed V14 replay, but does not supply a separate independently verified final V15 build record;
4. the present execution environment has no `lean`, `elan`, or `lake`, so an independent kernel replay was unavailable.

This is a status/provenance limitation, not a mathematical objection to the exact algebraic identities.

## 4. Mathematical audit of the current research front

### 4.1 Exact source-minimal algebra

The identity

```math
Λ = μ * log
```

follows directly in the Dirichlet convolution ring from `μ*1 = ε` and `log = Λ*1`. Its affine specialization yields

```math
Λ(2mn+s) = Σ_{qr=2mn+s} μ(q) log r,  s = ±1.
```

The split into small-`q`, small-`r`, and hard pairs is exhaustive and disjoint. In the hard region,

```math
qr - 2mn = s = ±1
```

has the complete multiplicity-one affine parametrization

```math
q = q0 + 2mt,
n = n0 + rt.
```

These are exact algebraic reductions; no cancellation estimate follows merely from them.

### 4.2 Balanced-seven cell

For

```math
Q = X^(3/5),  Y = X^(1/7),
```

the raw seven labelled prime boxes admit an exact squarefree torus polarization. Repeated-prime tuples are lower order at `X^(6/7+o(1))`. The polarized multiplicative family belongs to the relevant class-C source class at the algebraic level.

The ordinary separate multiplicative large sieve and separated high moments both miss by `X^(1/10)`. The equal-seven three-block dictionary fails its literal range conditions. Prime-box decomposition and modulus micro-switches are not exhaustive.

The exact remaining balanced-seven compiler is:

```text
AFFINE287-POLARIZED-OMEGA7-SIGNED-EOD45
+
AFFINE287-MULOG-COMPARISON-LOWCOND-MATCH45
=>
AFFINE287-BALANCED7-MODULUS-AVERAGE45 CLOSED.
```

Both antecedents remain open.

### 4.3 Correct relationship to the older V14 route

The V14 Vaughan / prime-modulus Möbius two-outer route remains mathematically valid as an alternative source decomposition. V15's `Λ=μ*log` chain is source-minimal and should be presented first. Therefore V16 does not call the V14 packet false; it labels it an optional analytic child rather than the unique controlling source.

## 5. Presentation defects in V15

The prior PDF had five major public-facing problems:

1. The title described a common-weight Type-II/twin-prime programme rather than Erdős #287.
2. V14 and V15 updates were inserted immediately after `\maketitle`, before the inherited abstract.
3. The abstract and table of contents described an older Draft-13 architecture rather than the current #287 result/frontier.
4. The stable filename contained `V2_FINAL` while the internal content was V15.
5. Machine-checked algebra, conditional compilers, source-open interfaces, false dictionaries, and open analysis were interleaved chronologically rather than classified by proof status.

## 6. V16 repairs

V16 now:

- titles the paper directly as an Erdős #287 finite reduction and large-`M` criterion;
- starts with an explicit status firewall;
- places a proof-status table before the table of contents;
- states the exact finite theorem and `WindowPairSupply` compiler first;
- presents current algebra continuously rather than as V14/V15 patches;
- displays one current dependency graph;
- moves failed routes, Gate nomenclature, source archaeology, and chronology into the technical ledger;
- distinguishes public Lean status from midpoint V15 formalization source;
- preserves the old V15 PDF/source immutably;
- introduces a canonical current filename while keeping the forum's legacy URL working.

## 7. Publication rule after V16

A future result may be promoted to **PUBLIC LEAN-CHECKED** only after:

1. the exact source is merged into public `main`;
2. `lake build` passes on that commit;
3. the axiom/prohibited-token audit is rerun;
4. theorem names and files are pinned in the public status map.

An analytic claim may be promoted to **PROVED** only by a proof in the paper or an exact published theorem with a literal source/range/normalization dictionary. Similarity of notation, capacity calculations, or an uninhabited Lean structure is not enough.

## 8. Final audit verdict

```text
V15: excellent chronological verification dossier;
V16: appropriate public mathematical paper;
finite theorem: public Lean-checked;
V15 algebra: exact/formalization source present, public replay pending;
analytic completion: open;
Erdős #287: open.
```
