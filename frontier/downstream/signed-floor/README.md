# Signed floor

**Status: OPEN.**

The signed-floor stage is downstream of the current `R_012` parent. Existing formal compiler structure and local cancellation banks do not constitute a populated physical signed-floor certificate.

Entry guard: do not claim this stage entered or closed merely from the banked `B_src` normalization, half-divisor algebra, diagonal-zero identities, or candidate `j`-sector reductions.
