# FRONTIER / current controlling parent

**Global verdict: OPEN.**  
**Evidence class:** live proof-search synthesis; candidate reductions remain below the audited R12 bank until independent audit.

The current proof-search route no longer treats the former `F/ED/DD/S -> all-q -> medium-k -> two-high` sequence as a mandatory chain of independent theorems. Those coordinates remain historically meaningful, but the live source has been recompiled into one signed selector parent.

## Current parent

```text
corrected selector Γ
        |
        +-- j >= 4      CANDIDATE-EMPTY / range census
        |
        +-- j = 3       CANDIDATE-CLOSED: |R_3| < 3e-9 B_X
        |               independent hostile audit still required
        |
        +-- j = 0
        +-- j = 1
        +-- active j=2
                |
                v
        R_012 := signed (j=0 + j=1 + active j=2) parent
                |
                +-- complete low-divisor fibres
                |       exact cancellation in the current source chart
                |
                +-- incomplete low-divisor fibres     OPEN
                +-- rough defect                      OPEN
                +-- selector-sensitive Perron/hyperbola
                |       diagonal-zero repair available as a candidate
                |       large ratio-frequency decay OPEN
                |
                +-- active j=2 source census
                        c=1: 71 prefix families
                        c=2: 37 prefix families
```

The exact current wall is therefore **not** “prove medium-`k` first”. It is the source-exact signed `R_012` parent, including its incomplete-fibre / rough-defect / large-ratio-frequency obligations.

## Diagonal-zero status

The full unselected parent retains the banked diagonal identity `Z(s,s)=0` at its stated scope. For the active selector `Theta`, the live source develops a defect

```text
Z_{Theta,c}(s,s) = -H_c(s).
```

The latest candidate repair completes it by

```text
Ztilde_c = Z_{Theta,c} + H_c,
Ztilde_c(s,s) = 0,
```

with a reported finite-height auxiliary correction `< 2.4e-13 B_X`. This repairs the **diagonal zero**, not the remaining large ratio-frequency / incomplete-fibre estimate. It is therefore recorded as a candidate subresult, not as closure of `R_012`.

## Promotion rule

No candidate line above is moved into [`../../banked/`](../../banked/) merely because it is newer. Promotion requires source reconciliation, constant/ledger audit where applicable, and explicit update of [`../../CLAIMS_LEDGER.md`](../../CLAIMS_LEDGER.md).
