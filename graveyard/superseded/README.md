# GRAVEYARD / superseded

These objects can remain mathematically useful as coordinate charts or historical derivations, but they are no longer independent obligations on the live dependency path.

## Superseded as independent frontiers

- `F / ED / DD / S` covariance decomposition;
- all-`q` discrepancy as a separate next theorem;
- moving Ramanujan coefficient/window formulations;
- the static-Ramanujan `r>200` snapshot as the controlling top-level coordinate;
- medium-`k -> two-high` as a mandatory sequential DAG.

The current live chart is the signed selector parent

```text
j=0 + j=1 + active j=2
```

with `j=3` and `j>=4` recorded separately as candidate reductions.

**Superseded does not mean proved.** The mathematics represented by an old coordinate is either absorbed into the new parent, remains available as a lemma/diagnostic chart, or survives as an open subobligation of that parent.

Historical files remain under [`../../archive/`](../../archive/) or their legacy source paths recorded by the archive index.
