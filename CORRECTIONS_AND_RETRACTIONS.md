# Erdős #287 — Corrections and Retractions

Append-only public correction ledger. Historical files are not deleted; later checkpoints supersede earlier controlling claims where stated.

## 31 August 2026

### RETRACTED — C0 post-conditioning index architecture

The old controlling use of

```text
n = j inverse(u') mod x
```

is retracted as the C0 architecture after canonical reduced-projective cancellation.

### RETRACTED — old C0 wrap

The old wrap

```text
1 + UU'/B
```

is retracted as the controlling finite estimate. The audited finite Fourier contraction contains the four terms

```math
\eta_b^2 \ll L^C\left(\frac{x}{B}+\frac1N+\frac1{E_b}+\frac1x\right).
```

### RETRACTED — naïve transverse two-signless DFT

The naïve full-CRT two-signless-carrier DFT is retracted. Full CRT fusion of `q_C` and `q_m` produces cross-modulus inverse coefficients in the numerator, so the transverse phase is not generally a fixed signless kernel of the previously advertised form.

### SUPERSEDED — transverse pair-existence as first frontier

```text
THREEFACTOR-TRANSVERSE-CARRIERFACTORIZATION-PAIR-EXISTENCE45
```

is preserved historically but superseded as the first transverse frontier.

The current first analytic residual at that historical checkpoint was

```text
THREEFACTOR-TRANSVERSE-CRITICAL-BEZOUTNUMERATOR-SINGLECARRIER45 : OPEN
```

This is no longer the current public frontier; it remains here as a dated correction record.

### SUPERSEDED AS CONTROLLING PUBLIC FRONTIER — 30 August uniform-k0 residual

The R7 label

```text
287-K0-SP2-DET1-SHAREDGCD-ONELEVEL-MOBIUS-GRAM45
```

is preserved in R7 as historical research development, but the 31-August C0/transverse audit superseded it as the controlling public first frontier. This is a status/frontier supersession, not a claim that every local identity in the R7 route is false.

---

## 5 September 2026 — audited R12 effectivity corrections

### RETRACTED — unweighted per-fibre `C_joint <= 0.09`

The unweighted joined covariance bound is false. A one-point physical fibre gives

```text
C_joint >= 9/64 > 0.09.
```

The surviving physical norm retains the outer `lambda(b)` weight. The audited residual is therefore a weighted covariance problem, not the retired unweighted theorem.

### RETRACTED — per-prime insertion of a global singular-series factor

The global singular-series factor is not independently available at each prime coordinate of the physical source. Treating it as a per-prime multiplicative factor is source-mismatched.

### RETRACTED — double charging `V<50` and `V<1000`

The new `V<1000` finite edge replaces the earlier `V<50` finite charge. The old charge must be removed before the new one is inserted; retaining both is double counting.

### FIREWALL — full half-divisor chart versus incremental medium chart

These are alternative decompositions. Their charges cannot be combined as if they were disjoint simultaneous contributions.

### FIREWALL — `Z(s,s)=0` does not by itself control directed Perron truncation

The diagonal Euler-product cancellation is an exact algebraic fact at its stated scope. It does not by itself estimate the directed anti-diagonal Perron truncation or justify the relevant contour move.

### FIREWALL — complete-period endpoint is not the complete joined covariance

The complete-period endpoint main is closed at its stated scope. It is not identical to the complete finite-period joined covariance, which retains the physical weighted residual.

### CURRENT AUDITED RESIDUAL

After the R12 audit the controlling weighted covariance allowance is

```math
C_F + 2C_{ED} + C_{DD} + C_S < \frac{283}{37500}.
```

The factor `2` on `C_ED` and the physical outer `lambda(b)` weight are mandatory.

The R12 audited capacity ledger is

```text
certified subtotal:  5.257263872e-7
remaining capacity:  3.603386128e-7
```

Newer candidate numbers are not promoted into this ledger until independently audited.

### CURRENT DOWNSTREAM FIREWALL

```text
medium-k:              STRICTLY REDUCED / OPEN
two-high:              OPEN / NOT ENTERED
signed floor:           OPEN
Maynard effectivization:OPEN / NOT ENTERED
Erdős #287:             OPEN
```

### PUBLICATION FIREWALL

Nothing in this correction ledger constitutes a proof of Erdős #287. The problem remains **OPEN**.
