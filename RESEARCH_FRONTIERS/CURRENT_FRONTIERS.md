# CURRENT RESEARCH FRONTIER — ERDŐS #287

Last reconciled: 2026-09-01

Purpose: authoritative current-state locator for **Erdős Problem #287 only**. This is not itself a proof. Historical provenance belongs in append-only proof indexes and archived reports.

## Public status

```text
ERDOS287: OPEN
```

## Newly banked physical endpoint

For the literal Balanced7 physical source, the `2+5` split is

```text
m = p1*p2,
w = p3*p4*p5*p6*p7,
q | 2*m*w+s  <->  w = -s*(2*m)^(-1) mod q.
```

The direct centered raw-minus-principal variance gives at research/paper level

```text
per dyadic Q:
    |D_{Q,s}| << X L^(-5/2+o(1));

full super-square-root band:
    sum_Q |D_{Q,s}| << X L^(-3/2+o(1)) = o(X/L).
```

Therefore the source-specific research bank is

```text
SP2-SUPERSQRT-TO-INVERSESAMPLED3221-PHYSICAL-DICTIONARY45:
    PASS.

SP2-BALANCED7-SUPERSQRT-DIRECT3221-DEFECT45:
    PAPER / RESEARCH CLOSED.

SP2-BALANCED7-FULL-Q45:
    PAPER / RESEARCH CLOSED.

BALANCED7:
    PAPER / RESEARCH CLOSED.
```

This is a source-specific physical endpoint. It does not inhabit the stronger generic arbitrary-log-power Type-II socket.

## Stronger generic socket — still open

```text
SP2-LABELLED-SINGLETON-CENTERED-QCELL-SUPERSQRT45:
    OPEN in the literal X L^(-A) for every fixed A formulation.

SP2-LABELLED-SINGLETON-GENERATEDTYPEII45:
    OPEN as a generic reusable theorem.
```

The direct physical estimate must not be promoted into either statement.

## Current first exact residual

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45
```

**Status:** `OPEN`.

The remaining task is no longer the physical Balanced7 super-square-root cell. It is the **source-exhaustive K0-SP2 fragment census and reassembly**.

Every physical fragment must be assigned exactly one owner, with explicit overlap/multiplicity accounting. The reassembly must preserve centering and must not multiply independent savings from separate owners.

## Controlling logarithmic budget

The newly banked physical endpoint has total super-square-root size

```text
X L^(-3/2+o(1)).
```

The final physical criterion requires

```text
o(X/L),
```

so the naive absolute reassembly budget is only

```text
C_total < 1/2
```

additional logarithmic cost. Equivalently

```text
net exponent = -3/2 + C_total < -1.
```

If `C_total >= 1/2`, the correct next move is joint/pre-triangle reassembly, not a claim that the number of fragments is merely `polylogarithmic`.

## Exact next audit

For the full K0-SP2 physical source:

1. print the exact finite source identity;
2. enumerate every fragment and overlap;
3. prove source exhaustivity;
4. assign one analytic owner to every fragment;
5. compute the exact fragment multiplicity exponent;
6. compute every external log cost;
7. verify uniformity of the direct short-shift-sieve and Shiu provider dictionaries on every routed Balanced7 fragment;
8. prove one full estimate `E_K0_SP2(X)=o(X/L)`;
9. only then promote `FULL-SOURCE-LOCAL-ANALYTIC-KERNEL45` if no source family remains.

## Downstream graph

Current safe graph:

```text
physical Balanced7 direct3221 endpoint
        [PAPER / RESEARCH CLOSED]
             |
             v
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45
        [OPEN]
             |
             v
FULL-SOURCE-LOCAL-ANALYTIC-KERNEL45
        [OPEN]
             |
             v
WindowPair or a direct final compiler
        [NOT YET ESTABLISHED]
             |
             v
ERDOS287
        [OPEN]
```

`WindowPair` must not be assumed necessary if a direct deterministic route to the original statement exists; the exact repository theorem graph must decide that question.

## Formal frontier

The 1-Sep Lean delta banks deterministic objects only:

- direct centered-defect data;
- raw/raw shift geometry;
- uninhabited analytic-provider interfaces;
- generic-vs-physical endpoint firewalls;
- K0-SP2 fragmentation metadata;
- symbolic log-budget lemmas;
- status and axiom-audit modules.

It does not turn external analytic estimates into axioms and does not prove Erdős #287.

## Scope firewall

This living frontier is Erdős-287-only. Cross-project material is not part of the current dependency graph or theorem ownership.
