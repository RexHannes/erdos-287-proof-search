# NEXT 287 RUN — POLARIZED OMEGA7 SIGNED ENDPOINT EOD

**Controlling V-branch target:** `AFFINE287-POLARIZED-OMEGA7-SIGNED-EOD45`

Do mathematics. Preserve the exact torus coefficient and the signed Möbius modulus average.

## Literal source

Use only

\[
Q=X^{3/5},\qquad Y=X^{1/7},
\]

and the multiplicative family

\[
a_z(p)=\frac17\sum_{i=1}^{7}z_i\omega_i(p),\qquad
f_z(p)=a_z(p)\ (p\sim Y),\qquad f_z(p^\ell)=0\ (\ell\ge2).
\]

The required quantity is

\[
\int_{\mathbb T^7}\overline{z_1\cdots z_7}
\sum_{q\sim X^{3/5}}\mu(q)
\Delta_{\rm LC}(f_z;X,q,-s\overline2)\,dz.
\]

Do **not** enlarge this to an absolute modulus average over all `z` unless the enlargement is proved lossless at the required scale.

## Bank / do not reopen

- character parent exact orthogonality;
- separate multiplicative large sieve is nonclosing by `X^(1/10)`;
- separated high moments are nonclosing by the same deficit;
- equal-seven P6.3 three-block dictionary is false;
- termwise prime-box HB/P6.3 repair is nonexhaustive;
- modulus micro-switch is nonexhaustive because the prime-q sector survives;
- repeated-prime tuples are `O(X^(6/7+o(1)))`;
- squarefree torus polarization is exact;
- `f_z` is uniformly in the smooth multiplicative class.

## Required binary outcome

Either:

1. prove `AFFINE287-POLARIZED-OMEGA7-SIGNED-EOD45`; or
2. give a literal dictionary to a published theorem with every endpoint/smoothness/exceptional-character condition checked; or
3. print a strictly narrower first residual with exact formula, ranges, normalization and required saving.

Separately audit the comparison pin

`AFFINE287-MULOG-COMPARISON-LOWCOND-MATCH45`.

Do not claim balanced-seven closed unless both the signed EOD theorem and the comparison low-conductor match are proved.

Keep the M/Vaughan branch `AFFINE287-PRIME-MODULUS-MU-TWOOUTER45` separate; this run is V-branch only.
